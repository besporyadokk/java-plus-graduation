spring:
  application:
    name: gateway-server
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      fail-fast: true
  config:
    import: "configserver:"


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/