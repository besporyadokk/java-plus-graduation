server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://core-db:5432/core_services?currentSchema=user_service
    username: dbuser
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
      default_schema: user_service
    show-sql: false

logging:
  level:
    ru.practicum: DEBUG