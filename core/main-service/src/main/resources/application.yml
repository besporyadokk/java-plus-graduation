spring:
  application:
    name: main-service
  cloud:
    config:
      uri: http://localhost:8889
      fail-fast: true
  config:
    import: "optional:configserver:"

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/